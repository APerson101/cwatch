import 'package:flutter/material.dart';

class PartnersView extends StatelessWidget {
  const PartnersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('partners Page')),
    );
  }
}
